import React from "react";
import { ProfileLayout } from "../../layouts";

type Props = {};

const RecentProductsPage = (props: Props) => {
  return (
    <ProfileLayout title="Sản phẩm đã xem">RecentProductsPage</ProfileLayout>
  );
};

export default RecentProductsPage;
