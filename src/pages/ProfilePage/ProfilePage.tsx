import React from "react";
import { ProfileLayout } from "../../layouts";

type Props = {};

const ProfilePage = (props: Props) => {
  return <ProfileLayout title="Thông tin tài khoản">ProfilePage</ProfileLayout>;
};

export default ProfilePage;
